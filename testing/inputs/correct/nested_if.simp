;-------------------------------------------------------------------------------
; nested in if
;-------------------------------------------------------------------------------

name()() {

    if(comp.name == 0) {
        bulart = 0
        if(comp.name == 0) {
            bulart = 0
            if(comp.name == 0) {
                bulart = 0
            }
            else {
                goop()()
            }
        }
        else {
            goop()()
        }
    }
    else {
        goop()()
    }
}
