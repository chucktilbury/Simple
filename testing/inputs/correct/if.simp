;-------------------------------------------------------------------------------
; Basic if/elif/else clause
;-------------------------------------------------------------------------------

name()() {

    if(required.expression) {
        some_func()()
    }
    else(another.required.expression) {
        a.different.thing()() ; the word func is illegal in a reference
    }
    else {
        ; must not have an expression
        const string name1
    }
}
