;-------------------------------------------------------------------------------
; basic else with if
;-------------------------------------------------------------------------------

name()() {

    if(comp.name == 0) {
        bulart = 0
    }
    else((nubbs+123) == 0) {}
    else {
        platy()()
    }
}
