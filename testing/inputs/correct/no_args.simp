;-------------------------------------------------------------------------------
; Test using a formatted string with no args in a string expression
;-------------------------------------------------------------------------------

string some_name = 'this is a string' ; not a formatted string.
string some_name = "this is a string"
